import { SessionCondidature } from './session-condidature';

describe('SessionCondidature', () => {
  it('should create an instance', () => {
    expect(new SessionCondidature()).toBeTruthy();
  });
});
